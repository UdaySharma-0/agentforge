{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "startCommand": "python -m nltk.downloader -d ./nltk_data wordnet omw-1.4 && uvicorn app.main:app --host 0.0.0.0 --port $PORT",
    "healthcheckPath": "/"
  }
}
