services:
  - type: web
    name: ai-agent-service
    runtime: python
    rootDir: ai-agent-service
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
